require '../spec-helper'

describe "contexts/edit", ->
  Edit = require '../../lib/contexts/edit/edit-context'
  describe '#initState', ->
    it 'returns initial state' #, ->
      # TODO stub database

  describe '#expandComponentProps', ->
    it 'returns templateProps' #, ->
      # TODO stub database
