require '../spec-helper'

describe "contexts/main", ->
  Main = require '../../lib/contexts/main/main-context'
  describe '#initState', ->
    it 'returns initial state' #, ->
      # TODO stub database

  describe '#expandComponentProps', ->
    it 'returns templateProps' #, ->
      # TODO stub database
