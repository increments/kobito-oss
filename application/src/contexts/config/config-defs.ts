export interface Props {
  endOfOldVersion: boolean;
}

export interface State {
  content: any;
  showDebugMenu: boolean;
}

// output format to template
export interface Template {
  content: any;
  showDebugMenu: boolean;
}
